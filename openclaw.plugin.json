{
  "id": "canvas-lms",
  "name": "Canvas LMS",
  "description": "Canvas LMS integration - courses, assignments, grades, and ICS schedule cross-reference",
  "configSchema": {
    "type": "object",
    "required": ["canvasApiToken", "canvasBaseUrl"],
    "properties": {
      "canvasApiToken": {
        "type": "string",
        "description": "Canvas API access token"
      },
      "canvasBaseUrl": {
        "type": "string",
        "description": "Canvas instance URL, e.g. https://canvas.instructure.com"
      },
      "scheduleIcsUrl": {
        "type": "string",
        "description": "Optional ICS calendar URL for schedule cross-reference (TimeEdit, Google Calendar, etc.)"
      },
      "keywordMap": {
        "type": "array",
        "description": "Optional keyword pairs [assignmentKeyword, scheduleKeyword] for crossref matching. If omitted, fuzzy word matching is used.",
        "items": {
          "type": "array",
          "items": { "type": "string" },
          "minItems": 2,
          "maxItems": 2
        }
      }
    }
  }
}
